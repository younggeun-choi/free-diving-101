import { View } from 'react-native';
import { Text } from '@/shared/ui/text';

export default function EqualizingScreen() {
  return (
    <View className="flex-1 justify-center items-center bg-white">
      <Text variant="h2">이퀄라이징 훈련</Text>
    </View>
  );
}
